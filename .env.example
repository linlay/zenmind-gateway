## 网关服务监听端口，通常映射到宿主机用于本地访问。
## Gateway service listen port, usually exposed on the host for local access.
GATEWAY_PORT=11945

## 后端运行模式：`host` 表示后端运行在宿主机，`container` 表示后端运行在容器内。
## Backend runtime mode: `host` means the backend runs on the host, `container` means it runs inside a container.
AP_BACKEND_MODE=container

## 终端后端运行模式：`host` 表示连接宿主机服务，`container` 表示连接容器内服务。
## Terminal backend runtime mode: `host` connects to a host service, `container` connects to a service inside a container.
TERM_BACKEND_MODE=host

## 网盘后端运行模式：`host` 表示连接宿主机服务，`container` 表示连接容器内服务。
## Pan backend runtime mode: `host` connects to a host service, `container` connects to a service inside a container.
PAN_BACKEND_MODE=host
